#include<iostream>
using namespace std;
int main()
{
int num;
cout <<"enter the number:-"  << endl;
cin>>num;
if(num==0)
cout<<"the number is zero---(0)";
else if(num>0)
cout<<"the number is positive---(1)";
else if(num<0)
cout<<"the number is negative---(-1)";
return 0;
}
